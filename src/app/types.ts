// types.ts

export interface Book {
    id: string;
    title: string;
    authors: string[];
    publishedYear: number;
    coverImageUrl: string; // Adjust based on your data structure
    rating: number; // If applicable
  }
  